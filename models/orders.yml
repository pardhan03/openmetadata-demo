version: 2
models:
  - name: orders
    description: "All customer orders"
    meta:
      owner: "data-team@company.com"
    tags: [finance]
    columns:
      - name: order_id
        description: "Primary key"
        tags: [PK]
      - name: customer_id
        description: "Test-5 Again"
        tags:
          - PII
          - SENSITIVE
